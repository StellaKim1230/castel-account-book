{
  "name": "castel-account-book",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"pnpm --filter=castel-server start\" \"pnpm --filter=castel-web dev\"",
    "build": "concurrently \"pnpm --filter=castel-server build\" \"pnpm --filter=castel-web build\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
